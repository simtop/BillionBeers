androidModule(true)

apply plugin: 'androidx.navigation.safeargs'

dependencies {
    implementation Libs.navigation_fragment_ktx
    implementation Libs.navigation_ui
    implementation project(Modules.beer_domain)
    implementation project(Modules.beers_list)
    implementation project(Modules.beer_detail)
    implementation project(Modules.core)
    implementation project(Modules.beer_data)
    implementation project(Modules.beer_database)
    implementation project(Modules.beer_network)
    implementation project(Modules.presentation_utils)

    testImplementation "io.mockk:mockk:1.11.0"
    testImplementation "io.strikt:strikt-core:0.24.0"

    androidTestImplementation "io.strikt:strikt-core:0.24.0"
}